# process_external_delivery_queue

Guide-aligned `photo_studio` delivery plugin that lists and updates the local-shadow external export queue.

The current staged contract also supports a bounded `publish_record` action with explicit `dry_run` / `live` execution modes.
