# 腾讯云 next 源码整理与发布回执

验证时间：2026-09-13T12:06:49.139224+00:00。Jenn 授权先整理 next 并推送自己的 GitHub，指定新分支 `nuobao-vcptoolbox`。目标为 `JENN2046/VCPToolBox`。

## 范围和来源

这是独立目录中准备的源码快照：来源 HEAD `1dbbd29edf05254935c27a315eedca3a3f049d99` 叠加 next 的工作目录源码。现有暂存区不作为发布清单，因为其中包含本机配置／状态路径；仅按经过核对的源码名单取材。

父提交取原远端 main `e5874076cf7946911815ac100bb2027038a6cc73`，不把腾讯云未审查的本地 Git 历史整体上传。未吸收其他分支或其他机器的未提交修改。原 main 和 prod/stable 保持不变；本分支不是自动合并建议。

保留目标仓库 AGENTS.md 和已有 GitHub 工作流。原腾讯云服务、工作目录、分支、暂存区、配置、记忆和任务产物未改动。没有部署、重启或权限扩张。next 对 core/.git 的依赖保持原状。

## 整理内容

- 将后端、前端、插件／桥接、Rust 源码、测试、依赖锁文件及允许的模板纳入明确的发布范围。
- Git 忽略规则补齐嵌套日记、知识、任务产物、生成文件、私有运行提示词、真实配置及认证数据边界。
- 编译产物不纳入源码分支，保留对应构建源码；前端在独立目录构建验证。
- CodexWorker 执行器及产物留待外置执行层整理；本次未整包纳入 VCP 核心。
- GitOperator 的原 repos.json.example 触发凭据形态检测，原件未发布；另写空 profiles 模板，不含身份或认证信息。
- 新增 SOURCE_STATUS.md 说明来源、使用边界及与生产目录的区别。

## 验证结果

| 检查 | 结果 | 限制 |
|---|---|---|
| 发布路径和凭据形态检查 | PASS | 2013 个已暂存文件检查时无禁入路径、匹配的凭据形态或符号链接；启发式检查不是全面安全审计 |
| 来源源码变化检查 | PASS | 导出后逐文件哈希复核，未发现来源源码发生变化 |
| 核心 JS/MJS/CJS 语法 | PASS，182 个文件 | 不替代行为验证 |
| 后端重点回归 | PASS，336 通过、1 跳过 | 25 个测试文件，覆盖审批、接入、插件生命周期、恢复契约及心跳等；不是完整测试通过 |
| 桥接模块 npm test | PASS，20 项 | 使用现有安装的依赖，只运行隔离测试 |
| 前端 npm run build | PASS | 类型检查与 Vite 构建成功；产物未发布 |
| 前端完整 npm test | FAIL | 11 个测试文件失败、7 个通过；16 个测试失败、29 个通过，另有 1 个未处理错误。缺失 pointerDomTestUtils 在源 next 中也不存在，另有旧断言／mock 与实现不匹配 |
| 后端完整 npm test | INCOMPLETE / FAIL | 存在 OpenHerPersona、预处理顺序和 TDB lifecycle 失败，以及接入／恢复测试超时；有界重跑在 150 秒总时限停止 |
| 工作区 validate-workspace | PASS | 原工作区必需路径存在 |
| 工作区 validate-extension-manifests | PASS | 仅原脚本发现范围，不代表全部插件运行已验证 |
| 工作区 validate-no-secrets | FAIL | 105 条风险路径匹配，可能重复；未读取对应配置内容，也未将这些路径纳入发布 |

测试使用现有依赖的只读链接和一个现有 Rust 原生模块作为验证输入；这些链接和二进制未纳入发布。未执行全新环境依赖安装、Rust 可复现构建、真实 provider 调用、正式服务健康验收或跨机器整合。前端和后端完整测试没有被描述为 PASS。

## 审阅及恢复依据

本轮人工式限定检查核对了发布名单、源代码哈希、模板替换、目标分支、CI／规则不变及测试结果；没有调用独立审阅 Agent 或声称运行治理 gate 已接通。GitHub 发布由 Jenn 当前明确指令授权，属于本次源码分支发布，不能扩展为部署或主线合并授权。

原运行代码和 Git 暂存状态保留；本次没有清空生产工作树。其他机器修改合入与生产工作树后续归位仍是单独工作。不要直接将本源码快照覆盖运行目录，或将对 main 的全部差异当作可直接合并的删除清单。
