# Git files
.git
.gitignore

# Node.js
node_modules
**/node_modules

# Docker files
Dockerfile
docker-compose.yml
.dockerignore

# Local language runtimes and build outputs. Native artifacts must be rebuilt
# inside the image so the target libc/ABI always matches the base image.
.venv/
.venv-*/
**/.venv/
**/__pycache__/
**/*.py[cod]
rust-vexus-lite/target/
rust-vexus-lite/target-*/
Plugin/DailyNoteSearcher/src/target/
AdminPanel-Vue/dist/

# Configuration files (to be mounted as volumes)
config.env
Plugin/*/config.env
Plugin/*/.env

# Cache and data directories (to be mounted as volumes or handled by named volumes)
Plugin/WeatherReporter/city_cache.txt
Plugin/VCPLog/log/
Plugin/*/state/
Plugin/*/cache/
Plugin/*/log/
Plugin/DailyNote/dailynote/
dailynote/
image/
DebugLog/
VectorStore/
VectorStoreTDB/
VCPAsyncResults/
tmp/
**/*.sqlite
**/*.sqlite3
**/*.sqlite3-*
**/*.db
**/*.db-*

# IDE and OS specific files
.vscode/
.idea/
*.swp
*~
Thumbs.db
.DS_Store

# Logs
*.log
logs/

# Test files if not needed in production
example.test.js
VCPChrome/
Plugin/JapaneseHelper/
